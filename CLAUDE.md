# Crack Tracking

## プロジェクト概要
構造物のき裂を写真から計測するツール群。用途の異なる2つが入っている。

1. **σ実測ツール（web/）** — き裂の追跡測定で「その機材・その現場で何mmの変化まで
   検出できるか」をブラウザで実測する。高所・非接触の追跡が主用途。端末を問わない。
2. **CrackScan（App/, Sources/）** — iPhone の LiDAR でき裂幅を実寸計測する iOS アプリ。
   LiDAR の実用距離は約3mなので、近接計測専用。

現在の主軸は 1。大谷石構造物のき裂追跡（高所・遠方）が当面の対象。

## ロードマップ

### Phase 1: 測定ノイズの把握
1. ✅ DIC（デジタル画像相関）による変位計測
2. ✅ 円形ターゲットのサブピクセル重心
3. ✅ σ 算出と検出限界（3σ）の提示
4. ✅ 模擬き裂ターゲットシート（A4原寸・既知量の検証用）
5. 🔶 実写での σ 検証（現地撮影待ち）

### Phase 2: 精度の底上げ
6. 🔶 レンズ校正（チェッカーボード）— 三脚の有無より効く
7. 🔶 コード化ターゲットによる自動識別
8. 🔶 大気ゆらぎの評価
9. ✅ 点群による斜め補正と面外のはらみ出し（写真との良いとこどり）
13. ✅ ノンターゲット2時期比較（変位マップ＋相関低下マップ）
14. ✅ 点群の2時期差分（面外＝浮き・はらみ出しの進行、5mm級）
15. ✅ 点群の3D差分（ICP位置合わせ＋C2C。傾き・面内ずれを伴うブロック化に対応）
16. ✅ ライブ計測セッション（σ収束で自動停止）・基準セットの端末保存・再訪の自動照合

### Phase 3: 運用
10. ✅ 経時管理（測点の登録・履歴・有意差判定）
11. 🔶 帳票出力
12. ✅ 温度による見かけの開閉の分離

## 技術スタック
- **web/** — 素の ES モジュール + Canvas。ビルド不要。Node で単体テスト
- **Sources/CrackCore** — Swift。ARKit 非依存の計測ロジック。`swift test` で検証
- **App/CrackScan** — Swift + SwiftUI + ARKit。XcodeGen で生成

## アーキテクチャ

### 計測ロジックを必ず分離する
プラットフォーム API に依存したコードは実機なしで一行も検証できない。
`web/src/*.js` と `Sources/CrackCore` はどちらも
**入出力が数値だけの純粋ロジック**にしてあり、合成画像による回帰テストが書ける。
実際、この分離のおかげで実バグを2件（リッジ検出の極性・平面フィットの法線反転）
実機に触れる前に発見できている。

### ディレクトリ構成
```
web/
  index.html          σ実測ツール本体（カメラ風UI・スクロールしない）
  targets/index.html  模擬き裂ターゲットシート（A4原寸印刷）
  manual/index.html   取扱説明書（現地用・オフライン・印刷可）
  src/
    dic.js            ZNCC探索 → 逆合成Gauss-Newton
    targets.js        円形ターゲットの検出と輝度加重重心
    transform.js      アフィン／ホモグラフィのフィット
    sigma.js          σ算出と検出限界
    speckle.js        DIC適性（MIG）とピント判定
    pointcloud.js     PLY/LAS読込・ロバスト平面フィット・面外マップ
    surface.js        平面 → 写真の mm/px（斜め補正）・剥落リスク
    cloudpanel.js     点群パネルのUI
    change.js         2時期変化抽出（時期またぎDIC・相関低下）
    comparepanel.js   比較パネルのUI
    cloudchange.js    点群2時期差分（位置合わせ・面外変化の有意判定）
    cloudalign.js     点群3D位置合わせ（ボクセル最近傍・トリムICP）と C2C 差分
    clouddiffpanel.js 点群差分のUI（比較シート後半・平面法/3D法の切替）
    capture.js        ライブ計測セッションの判断（自動停止・合否・簡易σ）
    capturepanel.js   ライブ映像・セッション・再訪の自動照合（矢印誘導→自動開始）
    store.js          基準セットの端末保存（IndexedDB）
    history.js        測点・2時期比較・傾き・温度分離
    historypanel.js   経時管理パネルのUI
    shell.js          カメラ風シェル（HUD・レール・シート）
    exif.js / image.js
  test/run.mjs        合成データによる検証（225件）

Sources/CrackCore/    Swift の計測ロジック
App/CrackScan/        iOS アプリ
docs/accuracy.md      精度の根拠と限界（実運用前に必読）
```

## 設計上の約束

- **極性を合わせる** — `darkTopHat` の出力では、暗かったき裂が明るい線になる。
  `RidgeDetector` には必ず `polarity` を明示する。誤ると芯線ではなく両脇に応答する。
- **座標系** — `CameraIntrinsics`/`Plane` は画像系（X=右, Y=下, Z=前方）。
  ARKit（X=右, Y=上, Z=後方）との変換は `DepthPlaneEstimator` の一箇所に集約。
- **線形光で測る** — 画素の半分がき裂に覆われたとき、線形光なら値が背景と亀裂の
  ちょうど中間になる。ガンマ符号化のままだと幅が系統的にずれる。
- **精度の主張には必ず検証を添える** — 合成画像で真値を与えて誤差を数値で示す。
  実写で未検証のものは「設計上の目標値」と明記する。
- **平面フィットに主成分分析を使わない** — 最小固有ベクトルは外れ値が1割入るだけで
  90° 飛ぶ。従属軸を3通り試して残差最小を採る回帰＋MAD で落とす。
  Swift と web の両方で同じ罠を踏んだ。
- **面外の符号は一箇所で決める** — 法線を視点側へ向ける処理を `fitWallPlane` に
  集約している。ここを外すと「はらみ出し」が点群ごとに反転する。
- **写真と点群に同じ量を測らせない** — 面内は写真（0.035mm）、面外は点群（5〜15mm）。
  重ねると精度の低い方に引きずられる。
- **図の色域はノイズではなく検出限界に合わせる** — ノイズの分布に合わせると
  測れていないばらつきが全面フルカラーになり、本物と同じ強さで描かれる。
- **2時期の差は √2 倍広い** — 差の分散は σ_A² + σ_B²。1日ぶんの 3σ で判定すると
  有意でない変化を進行と報告する。
- **温度分離は前回比較にも効かせる** — 効かせないと、傾きは「進行なし」なのに
  前回比較だけ赤く光る、という食い違いが必ず出る（夏冬交互に測れば起きる）。
- **温度係数は文献から借りない** — 石材・目地・拘束で実効値が変わる。
  測点自身のデータから重回帰で出し、係数の標準誤差ぶんは判定の限界に足す。
- **`null >= 0` は true** — σ 未入力のガードを比較演算子で書くと素通りし、
  σ が無いのに「有意」と表示される。`Number.isFinite` で書く。
- **2時期DICは予測ではなくワープで** — 並進サブセットは回転勾配に耐えない
  （1.5°で0.1px級の安定バイアス）。段階1の変換で画像ごと基準の幾何へ戻す。
- **フレーム間ばらつきは系統誤差を見ない** — 三脚で同構図なら補間バイアスは
  ばらつきに一切出ない。SE には系統誤差の床（0.02px）を必ず敷く。
- **段階1と補正の変換モデルは揃える** — ホモグラフィの反りはアフィンでは
  吸収できず、画面の角に偽の有意セルが並ぶ。
- **ロバスト刈りは残差ベクトルの中央値中心から** — 残差の「大きさ」のMADは、
  外れ値3割で当てはめごと引きずられると二群が重なって誰も刈れない。
- **周期模様の位置合わせは事前情報で縛る** — 目地は周期的なので、石1〜2個ぶん
  ずれた場所でも相関は同点になる。重心合わせを事前情報とし、ずれ量に
  ペナルティを付けて同点なら小さいずれを採る。付けないと差分に偽の対が出る。
- **段差をまたぐセルは限界を広げる** — 目地・稜線の上ではセル中央値が段差の
  どちら側に落ちるかが時期間で反転する。近傍との高低差ぶんを限界に足さないと、
  目地に沿って偽陽性が並ぶ。
- **数十セルの最大値は必ず上振れる** — 領域の代表値に最大セルを使うと
  σ×2〜3の過大報告になる（最大値統計）。ピーク周辺3×3の平均を使う。
- **C2C の 3D 距離を主値にしない** — 2スキャンの点は同じ場所に落ちないので、
  最近傍距離には点の間隔（9万点/14m² で約6mm）がそのまま乗る。主値は
  最近傍差の**法線成分**（面外はフル精度で出る）、3D距離は「形が変わった」の副指標。
  CloudCompare で C2C より C2M/M3C2 が勧められるのと同じ理由。
- **ICP も安定域だけで解く** — 動いたブロックを含めて合わせると、検出したい変位が
  剛体変換に吸われて過小になる。構造物の特徴を「マーカー」に流用するのは正しいが、
  流用してよいのは動いていない本体側の特徴だけ。
- **判断は人から取り上げ、動作だけ残す** — 枚数・停止・立ち位置の合否はアプリが
  決める（σ収束の自動停止、再訪の自動照合）。人がやるのは歩くことと押すことだけ。
- **探索半径はフレーム寸法に比例させる** — 固定 px の半径は縮小画像で探索点が
  全部画面外に落ち、confidence 0 で黙る。実際に再訪照合がこれで止まっていた。

## UI 方針
**ダークアウトドアテーマ（フォレストグリーン + アンバー）** — field-note-app と共通。
現場ツール群として見た目を揃える。数値は必ず等幅・tabular-nums。

**配置はカメラアプリ型**（Blackmagic Camera 参照）。常時ビューファインダー +
上に HUD + 右（縦持ちでは下）にレールとシャッター。詳細はシートで手前に出す。
- **判断に要る数値は常時画面に置く** — 縦に積むと、撮り直すべき状況に
  気づくのが現場を離れた後になる。
- **未評価と良好を同じ見た目にしない** — 「測っていない」と「問題なし」は違う。
  レールのドットは 灰=未評価 / 緑=良好 / 橙=注意 / 赤=不可。
- **grid/flex の自動最小値は min-content** — 折り返さないテキストが1つあるだけで
  枠ごと画面外へ広がる。`minmax(0, 1fr)` と `min-width: 0` で必ず潰す。
  実際これで σ ボタンが画面外へ押し出されていた。
- **主動作を無効化して黙らない** — σ ボタンは常に押せる。条件が足りなければ
  理由を出して、直せるシートまで連れて行く。灰色のまま反応しないのが最悪。
- **品質は「最後の1枚」ではなく「一番悪い1枚」を出す** — 1枚でもピントが
  外れていればセッション全体の σ が悪化する。平均や最新では見逃す。
- **HUD の飛び先は値の出どころに合わせる** — 距離は点群からも手入力からも来る。
  固定の飛び先にすると、押しても直せない場所へ飛ぶ。
- **UI の文言は実際の挙動に合わせる** — 「ドラッグを始めると自動で閉じます」と
  書いていたが、シートの背面がタップを飲むので事実と違った。実機で確かめて直す。

## コーディング規約
- コメントと UI は日本語
- web は素の ES モジュール（ビルドツールを増やさない）
- Swift は strict concurrency minimal、View は `@MainActor`

## 開発・実行
```bash
cd web && python3 -m http.server 8080   # σ実測ツール
cd web && node test/run.mjs             # 検証 225件
swift test                              # CrackCore 検証
xcodegen generate                       # iOS アプリの Xcode プロジェクト生成
```
