# Crack Tracking

## プロジェクト概要
構造物のき裂を写真から計測するツール群。用途の異なる2つが入っている。

1. **σ実測ツール（web/）** — き裂の追跡測定で「その機材・その現場で何mmの変化まで
   検出できるか」をブラウザで実測する。高所・非接触の追跡が主用途。端末を問わない。
2. **CrackScan（App/, Sources/）** — iPhone の LiDAR でき裂幅を実寸計測する iOS アプリ。
   LiDAR の実用距離は約3mなので、近接計測専用。

現在の主軸は 1。大谷石構造物のき裂追跡（高所・遠方）が当面の対象。

## ロードマップ

### Phase 1: 測定ノイズの把握
1. ✅ DIC（デジタル画像相関）による変位計測
2. ✅ 円形ターゲットのサブピクセル重心
3. ✅ σ 算出と検出限界（3σ）の提示
4. ✅ 模擬き裂ターゲットシート（A4原寸・既知量の検証用）
5. 🔶 実写での σ 検証（現地撮影待ち）

### Phase 2: 精度の底上げ
6. ✅ レンズ校正（平面格子）— ただし効くのは歪み×姿勢差。再訪が良好なら不要
7. 🔶 コード化ターゲットによる自動識別
8. 🔶 大気ゆらぎの評価
9. ✅ 点群による斜め補正と面外のはらみ出し（写真との良いとこどり）
13. ✅ ノンターゲット2時期比較（変位マップ＋相関低下マップ）
17. ✅ 点群による視差補正（立ち位置が違っても面内を測る）
14. ✅ 点群の2時期差分（面外＝浮き・はらみ出しの進行、5mm級）
15. ✅ 点群の3D差分（ICP位置合わせ＋C2C。傾き・面内ずれを伴うブロック化に対応）
16. ✅ ライブ計測セッション（σ収束で自動停止）・基準セットの端末保存・再訪の自動照合

### Phase 3: 運用
10. ✅ 経時管理（測点の登録・履歴・有意差判定）
11. 🔶 帳票出力
12. ✅ 温度による見かけの開閉の分離

## 技術スタック
- **web/** — 素の ES モジュール + Canvas。ビルド不要。Node で単体テスト
- **Sources/CrackCore** — Swift。ARKit 非依存の計測ロジック。`swift test` で検証
- **App/CrackScan** — Swift + SwiftUI + ARKit。XcodeGen で生成

## アーキテクチャ

### 計測ロジックを必ず分離する
プラットフォーム API に依存したコードは実機なしで一行も検証できない。
`web/src/*.js` と `Sources/CrackCore` はどちらも
**入出力が数値だけの純粋ロジック**にしてあり、合成画像による回帰テストが書ける。
実際、この分離のおかげで実バグを2件（リッジ検出の極性・平面フィットの法線反転）
実機に触れる前に発見できている。

### ディレクトリ構成
```
web/
  index.html          σ実測ツール本体（カメラ風UI・スクロールしない）
  targets/index.html  模擬き裂ターゲットシート（A4原寸印刷）
  calib/index.html    レンズ校正シート（A4原寸印刷・7×5=35点）
  testpattern/index.html 室内検証パターン（画面を変位ステージに使う）
  gauge/index.html    クラックゲージシート（CrackScan の幅計測を既知幅の線で検証・A4原寸）
  reset.html          更新して直す（キャッシュ一覧に**載せない**復旧ページ）
  manual/index.html   取扱説明書（現地用・オフライン・印刷可）
  src/
    dic.js            ZNCC探索 → 逆合成Gauss-Newton
    targets.js        円形ターゲットの検出と輝度加重重心
    transform.js      アフィン／ホモグラフィのフィット
    sigma.js          σ算出と検出限界
    speckle.js        DIC適性（MIG）とピント判定
    pointcloud.js     PLY/LAS読込・ロバスト平面フィット・面外マップ
    surface.js        平面 → 写真の mm/px（斜め補正）・剥落リスク
    cloudpanel.js     点群パネルのUI
    change.js         2時期変化抽出（時期またぎDIC・相関低下）
    comparepanel.js   比較パネルのUI
    parallax.js       立ち位置のずれが作る見かけの変位（視差）の推定と補正
    cloudchange.js    点群2時期差分（位置合わせ・面外変化の有意判定）
    cloudalign.js     点群3D位置合わせ（ボクセル最近傍・トリムICP）と C2C 差分
    clouddiffpanel.js 点群差分のUI（比較シート後半・平面法/3D法の切替）
    lenscal.js        レンズ歪みの推定（平面格子）と補正
    capture.js        ライブ計測セッションの判断（自動停止・合否・簡易σ）
    capturepanel.js   ライブ映像・セッション・再訪の自動照合（矢印誘導→自動開始）
    store.js          基準セットの端末保存（IndexedDB）
    history.js        測点・2時期比較・傾き・温度分離
    crackline.js      亀裂測点（線の両側の帯の相対変位 → 開口・ずれ・標準誤差）
    historypanel.js   経時管理パネルのUI
    shell.js          カメラ風シェル（HUD・レール・シート）
    exif.js / image.js
  test/run.mjs        合成データによる検証（310件）

Sources/CrackCore/    Swift の計測ロジック（Detection/AnalysisPlanner.swift: 目標幅 → 縮小率・解析範囲）
App/CrackScan/        iOS アプリ
docs/accuracy.md      精度の根拠と限界（実運用前に必読）
```

## 設計上の約束

- **極性を合わせる** — `darkTopHat` の出力では、暗かったき裂が明るい線になる。
  `RidgeDetector` には必ず `polarity` を明示する。誤ると芯線ではなく両脇に応答する。
- **座標系** — `CameraIntrinsics`/`Plane` は画像系（X=右, Y=下, Z=前方）。
  ARKit（X=右, Y=上, Z=後方）との変換は `DepthPlaneEstimator` の一箇所に集約。
- **線形光で測る** — 画素の半分がき裂に覆われたとき、線形光なら値が背景と亀裂の
  ちょうど中間になる。ガンマ符号化のままだと幅が系統的にずれる。
- **精度の主張には必ず検証を添える** — 合成画像で真値を与えて誤差を数値で示す。
  実写で未検証のものは「設計上の目標値」と明記する。
- **平面フィットに主成分分析を使わない** — 最小固有ベクトルは外れ値が1割入るだけで
  90° 飛ぶ。従属軸を3通り試して残差最小を採る回帰＋MAD で落とす。
  Swift と web の両方で同じ罠を踏んだ。
- **面外の符号は一箇所で決める** — 法線を視点側へ向ける処理を `fitWallPlane` に
  集約している。ここを外すと「はらみ出し」が点群ごとに反転する。
- **写真と点群に同じ量を測らせない** — 面内は写真（0.035mm）、面外は点群（5〜15mm）。
  重ねると精度の低い方に引きずられる。
- **図の色域はノイズではなく検出限界に合わせる** — ノイズの分布に合わせると
  測れていないばらつきが全面フルカラーになり、本物と同じ強さで描かれる。
- **2時期の差は √2 倍広い** — 差の分散は σ_A² + σ_B²。1日ぶんの 3σ で判定すると
  有意でない変化を進行と報告する。
- **温度分離は前回比較にも効かせる** — 効かせないと、傾きは「進行なし」なのに
  前回比較だけ赤く光る、という食い違いが必ず出る（夏冬交互に測れば起きる）。
- **温度係数は文献から借りない** — 石材・目地・拘束で実効値が変わる。
  測点自身のデータから重回帰で出し、係数の標準誤差ぶんは判定の限界に足す。
- **`null >= 0` は true** — σ 未入力のガードを比較演算子で書くと素通りし、
  σ が無いのに「有意」と表示される。`Number.isFinite` で書く。
- **2時期DICは予測ではなくワープで** — 並進サブセットは回転勾配に耐えない
  （1.5°で0.1px級の安定バイアス）。段階1の変換で画像ごと基準の幾何へ戻す。
- **フレーム間ばらつきは系統誤差を見ない** — 三脚で同構図なら補間バイアスは
  ばらつきに一切出ない。SE には系統誤差の床（0.02px）を必ず敷く。
- **段階1と補正の変換モデルは揃える** — ホモグラフィの反りはアフィンでは
  吸収できず、画面の角に偽の有意セルが並ぶ。
- **ロバスト刈りは残差ベクトルの中央値中心から** — 残差の「大きさ」のMADは、
  外れ値3割で当てはめごと引きずられると二群が重なって誰も刈れない。
- **周期模様の位置合わせは事前情報で縛る** — 目地は周期的なので、石1〜2個ぶん
  ずれた場所でも相関は同点になる。重心合わせを事前情報とし、ずれ量に
  ペナルティを付けて同点なら小さいずれを採る。付けないと差分に偽の対が出る。
- **段差をまたぐセルは限界を広げる** — 目地・稜線の上ではセル中央値が段差の
  どちら側に落ちるかが時期間で反転する。近傍との高低差ぶんを限界に足さないと、
  目地に沿って偽陽性が並ぶ。
- **数十セルの最大値は必ず上振れる** — 領域の代表値に最大セルを使うと
  σ×2〜3の過大報告になる（最大値統計）。ピーク周辺3×3の平均を使う。
- **C2C の 3D 距離を主値にしない** — 2スキャンの点は同じ場所に落ちないので、
  最近傍距離には点の間隔（9万点/14m² で約6mm）がそのまま乗る。主値は
  最近傍差の**法線成分**（面外はフル精度で出る）、3D距離は「形が変わった」の副指標。
  CloudCompare で C2C より C2M/M3C2 が勧められるのと同じ理由。
- **再訪ガイドは視差を止められない** — 小角度では並進と回転がほぼ縮退するので、
  画像が合っていても横に数十cm ずれていることがある。視差を作るのは実際の並進。
  合格条件は画面幅の 5%（24mm相当・5m で 37cm）で、視差の許容（凹凸20mmなら
  2.6cm）とは2桁違う。**歪みには効いたが視差には効かない**。
- **視差は測るのではなく解く** — 立ち位置を cm で測る機材（RTK）は要らない。
  面の形が分かっていれば、残差の場そのものが「どれだけずれたか」を語っている
  （未知数3つに対してセルが数十）。RTK は位置を測って視差を小さくし、点群は
  形を測って視差を計算で消す。手持ちの機材で取れるのは後者。
- **点群の視点は写真の視点ではない** — LiDAR は 3m しか届かないので、高所は
  近づいてスキャンして離れて撮ることになる。既定の「原点＝視点」を放置すると
  mm/px が 5倍狂い、視差補正は黙って無効になる（画面には「補正しました」と出る）。
  壊れるのは**奥行きだけ**で、横に 1m ずれても補正は効く（視線 11°）。だから
  撮影距離を1つ入れさせれば済む。歩測で足りる。
- **引いたのに減らないのは補正ではない** — 視差を当てても場が 10% 未満しか
  減らなければ、それは位置合わせが取れていない。成功として表示しない。
- **視差の補正は凹凸が全面にあるときだけ** — 凹凸が一部にしか無いと、そこに
  載っているセルの大半が本物に動いたブロックだったときに、当てはめが本物を
  視差と説明してしまう（合成検証で 1.0mm の本物を丸ごと消し、158mm のずれを捏造）。
  coverage と spread を測って足りなければ断る。ICP と同じで、
  **流用してよいのは動いていない側の特徴だけ**。
- **ICP も安定域だけで解く** — 動いたブロックを含めて合わせると、検出したい変位が
  剛体変換に吸われて過小になる。構造物の特徴を「マーカー」に流用するのは正しいが、
  流用してよいのは動いていない本体側の特徴だけ。
- **判断は人から取り上げ、動作だけ残す** — 枚数・停止・立ち位置の合否はアプリが
  決める（σ収束の自動停止、再訪の自動照合）。人がやるのは歩くことと押すことだけ。
- **探索半径はフレーム寸法に比例させる** — 固定 px の半径は縮小画像で探索点が
  全部画面外に落ち、confidence 0 で黙る。実際に再訪照合がこれで止まっていた。
- **レンズ校正は「戻れなかったときの保険」** — 歪み 2% でも再訪が良好（0.4°・画面幅の1%）
  なら偽陽性 0、姿勢差が中程度（2°・7%）だと 71 セル中 44。効くのは歪み単体ではなく
  **歪み × 姿勢差**。再訪ガイドは操作の省力化ではなく校正の代わりに効いている。
- **校正は歪みだけを解く** — 焦点距離は仕様値、主点は中心でよい。完全なカメラ校正は要らない。
  平面格子の曲がりは歪みでしか説明できないので1枚でも決まる（プラムライン法）。
  係数を仮に置けばホモグラフィは線形に厳密に解けるので、残差は係数だけの関数になる。
- **相関のある基底を1次元ずつ走査しない** — r² と r⁴ は形が似ていて残差の谷が斜めに伸びる。
  交互の1次元走査では谷を這い上がれず、k1 −0.05 を −0.042 と誤った。斜めに進める方法が要る。
- **ZNSSD が効くのは「一様な」明暗差だけ** — 全体が 1.6 倍明るくなっても偽陽性は
  出ないが、影の縁が動くとサブセット内で明るさの比が場所ごとに変わり、σ の 400 倍
  （1.1px 級）の偽変位が出る。本物と区別がつかない。小ブロックごとの比の散らばりで
  検出して落とす。**影が動かなければ害はない**ので、同じ時刻に撮るのが最良の対策。
- **照明の判定は変位ぶんずらして比べる** — 同じ座標で比べると、模様が 1px 動いた
  だけで小ブロックの平均が変わり、本物の変位を影と誤認する（仕込んだ 1.08px の
  ブロックが丸ごと消えた）。ずらせば剛体変位では明るさが一致する。
- **仕様に書いてある値を推定で置き換えない** — レンズの焦点距離は広角と望遠の画から
  相関で「実測」していたが、実機で明らかに誤った倍率を出した。相関の最大点は
  露出差・光軸のずれ・被写体の平面性で簡単にずれる。メーカーが 35mm 換算で
  公表している値（16 Pro: 13 / 24 / 120mm）を入れてもらう方式に戻した。
  測れるからといって、測る必要がないものを測らない。
- **キャッシュは2階層ある** — SW のキャッシュを消しても、ブラウザの HTTP キャッシュに
  古いモジュールが max-age のあいだ残る。片方だけ消すと「新しい HTML ＋ 古い app.js」に
  なり、起動時に黙って死ぬ。取り直すときは必ず `cache: 'reload'` で両方に効かせる。
- **古い版は自分を置き換えられないことがある** — キャッシュ優先の SW と、版番号で
  二重防止していた自動再読み込みが噛み合うと、新版を配っても端末が旧版から出られない。
  HTML の `data-version` とモジュールの版を突き合わせて自分で取り直す仕掛けと、
  キャッシュ一覧に**載せない**復旧ページ（reset.html）を出口として用意する。
- **飽和は sRGB で判定する** — 白飛び・黒つぶれは符号化の現象。線形光に戻したデータに
  sRGB のしきい値 0.02 を当てると、39/255 のただの暗いグレーが「黒つぶれ」に数えられ、
  風化した大谷石の正しく露出した写真が fair になった（実写で 12.6%、本当は 1.96%）。
  従うと本当に白飛びさせる。しきい値は線形光へ換算して持つ。
- **HUD の分解能は撮影解像度で出す** — ライブ映像（1920px 級）の内部パラメータで mm/px を
  計算すると、実際に計測する高解像度フレーム（4032px 以上）より 2 倍悪い値が出て、
  そこから逆算した「0.1m まで近づけ」は実現不能。高解像度フレームを 1 枚取って画素数を
  覚え、内部パラメータをその寸法へ拡縮する。分かるまでは「仮」と表示する。
- **助言は自分のブロック条件と矛盾させない** — 「近づいてください」と言う距離が最短距離
  （LiDAR の下限）より近いなら、それは行けない場所。その場合は目標幅の方を直してもらう
  （最短距離で測れる最小の幅を逆算して伝える）。実機初回で実際に矛盾した助言が出た。
- **亀裂は線で指定し、両側の帯の差を測る** — 非接触 DIC は変位の「場」しか出さないので、
  特定の亀裂を経時管理に載せるには場から数字を 1 つ取り出す経路が要る。亀裂に沿って
  引いた線の両側に、線から margin（サブセット半径以上）離した帯を取り、代表値（中央値）
  の差の法線成分＝開口、接線成分＝ずれ。線をまたぐサブセットは両側の中間値になって
  どちらの量でもないので必ず除く。符号は線の引き方に依存させない（接線を下向き／右向き
  に正規化）。基準日は 0 ± σ で記録し、次回の 2 時期比較で「基準からの変化」が乗る。
- **検出は縮小画像・計測は原寸** — 近接では 1mm の亀裂が 20px、3mm なら 60px に写る。
  等倍で追うと σ=30 級のカーネルが要って数十秒かかるうえ、目標幅 0.2mm 向けの
  σ 列では幅広の開口が両脇 2 本に割れる。芯線の位置は縮小画像で足りる（断面の
  対称性で中心を取り直す）ので、`AnalysisPlanner` が目標幅の px 数から縮小率を決め、
  幅だけ原寸の断面で測る。目標幅は「測りたい幅の級」（0.8〜4 倍を拾う）で、
  案件画面で後から変えられる。
- **画面の正規化座標と画像の正規化座標は別物** — 縦持ちではキャプチャ画像（横長）が
  90° 回って左右を切られて表示されるので、画面中央の正方形は画像上では横長の矩形。
  枠を画像座標のつもりで使うと、描いた枠と解析した範囲が食い違う。
  `displayTransform` で写し、`analysisRegion(for:)` の一箇所で決める。
- **描く枠は実際に解析する範囲** — 解析できる画素数の上限で中央だけ黙って切ると、
  48MP では枠の 4 割しか見ておらず「枠に入れたのに検出されない」になる。
  縮小率に応じた上限で縮めた範囲を画面へ戻して、それを枠として描く。
- **タップの探索半径は指の大きさから** — 固定 24px は 48MP で画面の 0.3%。
  画面の 28pt を `displayTransform` で画像 px に換算して渡す（300px 級になる）。
- **純 Swift の画像処理は Debug でも -O** — -Onone は配列添字ごとに関数呼び出しで
  10〜30 倍遅い。Xcode の ▶ は Debug なので、計測ロジック（CrackCore）だけ
  Debug でも最適化する。アプリ側の UI はデバッグしやすいまま。
- **高解像度フレームに深度が付いてこないことに備える** — 平面が取れなければ直前
  （1 秒以内）のライブ推定で代用し、その旨を結果に注記する。高解像度が取れなければ
  ライブ映像で測って注記する。黙って「取得できませんでした」で止めるのが最悪。
- **「全部測る」は暗くて細長いものを全部拾う** — 目地・斑点・影の縁、机の上なら
  キーボードの縁まで候補になる（実機で 13 本）。亀裂は人が**なぞって**指定し、
  なぞった線の近傍の芯線だけを、なぞった区間に限って測る。区間が重ならない断片は
  同じ亀裂の続きとして繋ぎ、重なる断片（並走する斑点・クラックスケールの隣の目盛り）は
  **なぞった線に近い方**だけ採る（長さで選ぶと隣の線が出る）。
  全部測るは補助に降格し、既定選択なし＋フィルタ（10mm 未満・幅の 8 倍未満・信頼度 0.4 未満を落とす）。
- **なぞり計測のしきい値は回廊の中で決める** — リッジのしきい値は応答のパーセンタイルで
  決めている。切り出し全体で取ると、なぞった亀裂の隣にもっと強い構造（目地・硬い縁）が
  あるだけで亀裂が種を持てず丸ごと消える（合成検証で並走する 4px の線を置いたら
  6px の亀裂が nil になった）。なぞった線の近傍だけで NMS 応答を残してから閾値を取る。
- **結果はライブ映像ではなく静止画の上に置く** — ライブに重ねると手が動くたびに線が
  ずれ、何を測ったのか確かめられない。撮った瞬間の画像・壁面・ブレ指標を
  `MeasurementStill` に固定し、その上でなぞる・拡大する・選ぶ。
- **手ブレは撮る前に待つ** — 計測の直前に動き（°/s。回転と距離で割った並進の大きい方）が
  収まるのを最大 1.5 秒待つ。撮った画像のブレ指標は、ライブ判定と同じ尺度にするため
  **ライブの画素密度まで縮小してから**出す。分解能の違う画像の指標をそのまま比べない。
- **1 本指はなぞり、2 本指は移動** — SwiftUI だけでは分けられない。UIScrollView の
  pan を 2 本指にし、1 本指の pan とタップをキャンバスに付ける。
- **12MP 級のビューに draw(_:) を実装しない** — その大きさのビットマップ（数百 MB）が
  確保されて落ちる。重ね描きは CAShapeLayer / CATextLayer で、線幅と文字はズームで割る。
- **回した表示画像の座標は一箇所で戻す** — 縦持ちの静止画は元画像を 90° 回して
  いる。なぞった座標を元画像へ戻す写像は `RotatedImageMapping` に集め、往復が
  一致することをテストで固定する。
- **人ごとに違うビルド設定は Local.xcconfig** — Team ID を project.yml に書くと
  `git pull` のたびに衝突する。gitignore した Local.xcconfig を、git 管理の
  Config/Base.xcconfig が `#include?` で任意に取り込む。project.yml から無いかも
  しれないファイルを直接指すと Xcode は "Unable to open base configuration reference
  file" で**ビルドを止める**（警告ではない。CI で実際に止まった）。

## UI 方針
**ダークアウトドアテーマ（フォレストグリーン + アンバー）** — field-note-app と共通。
現場ツール群として見た目を揃える。数値は必ず等幅・tabular-nums。

**配置はカメラアプリ型**（Blackmagic Camera 参照）。常時ビューファインダー +
上に HUD + 右（縦持ちでは下）にレールとシャッター。詳細はシートで手前に出す。
- **判断に要る数値は常時画面に置く** — 縦に積むと、撮り直すべき状況に
  気づくのが現場を離れた後になる。
- **未評価と良好を同じ見た目にしない** — 「測っていない」と「問題なし」は違う。
  レールのドットは 灰=未評価 / 緑=良好 / 橙=注意 / 赤=不可。
- **grid/flex の自動最小値は min-content** — 折り返さないテキストが1つあるだけで
  枠ごと画面外へ広がる。`minmax(0, 1fr)` と `min-width: 0` で必ず潰す。
  実際これで σ ボタンが画面外へ押し出されていた。
- **主動作を無効化して黙らない** — σ ボタンは常に押せる。条件が足りなければ
  理由を出して、直せるシートまで連れて行く。灰色のまま反応しないのが最悪。
- **品質は「最後の1枚」ではなく「一番悪い1枚」を出す** — 1枚でもピントが
  外れていればセッション全体の σ が悪化する。平均や最新では見逃す。
- **HUD の飛び先は値の出どころに合わせる** — 距離は点群からも手入力からも来る。
  固定の飛び先にすると、押しても直せない場所へ飛ぶ。
- **UI の文言は実際の挙動に合わせる** — 「ドラッグを始めると自動で閉じます」と
  書いていたが、シートの背面がタップを飲むので事実と違った。実機で確かめて直す。

## コーディング規約
- コメントと UI は日本語
- web は素の ES モジュール（ビルドツールを増やさない）
- Swift は strict concurrency minimal、View は `@MainActor`

## 進め方の約束

- **時間のかかる処理が終わったら通知する** — 検証は 310 件で 8〜10 分かかる。
  黙って待っていると席を外したときに気づけない。次の3つでは必ず通知を出す。
  検証の完了／公開（Pages への反映）の完了／判断待ちで止まったとき
- **確認していないことを報告しない** — 「配信されたはず」「動くはず」を事実として
  書かない。この案件では、公開されていないのに更新済みと報告する事故を起こした。
  外部に出したものは、出た先を見て確かめてから報告する

## 開発・実行
```bash
cd web && python3 -m http.server 8080   # σ実測ツール
cd web && node test/run.mjs             # 検証 310件
swift test                              # CrackCore 検証
xcodegen generate                       # iOS アプリの Xcode プロジェクト生成
```
